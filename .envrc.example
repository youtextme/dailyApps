# Devin / direnv — copy to .envrc (gitignored), fill values, run: direnv allow
# Same variable names as .env.example — one source of truth.

export SLACK_BOT_TOKEN="xoxb-replace-me"
export SLACK_APP_TOKEN="xapp-replace-me"
export SLACK_SIGNING_SECRET="replace-me"
export SLACK_CHANNEL_ID=""
export GITHUB_TOKEN="ghp_replace-me"
export TIRELESS_ROUTER_URL="http://127.0.0.1:8080"
